import React from 'react';

function Login(props) {
    return (
        <div>
            <p>Give username:</p>
            <input />
            <button>Button</button>
        </div>
    );
}

export default Login;